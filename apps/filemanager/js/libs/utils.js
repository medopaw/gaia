// Utils
// =============

define([], function() {
   var Utils = {
       errorHandler: function() {
           var msg = 'whatever';
           alert('Error: ' + msg);
       }
   };

   return Utils;
});
